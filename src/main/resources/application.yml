spring:
  profiles:
    active: dev

server:
  port: 8080

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.ironsync.entity
  type-handlers-package: com.ironsync.common.handler
  configuration:
    map-underscore-to-camel-case: true
